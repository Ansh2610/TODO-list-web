# Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to version control

# ===========================================
# Application Environment
# ===========================================
ENV=dev

# ===========================================
# API Configuration (FastAPI)
# ===========================================
API_TITLE="Resume Skill Analyzer API"
API_VERSION="1.0.0"
API_HOST="0.0.0.0"
API_PORT=8000

# ===========================================
# Security & CORS (REQUIRED FOR PRODUCTION)
# ===========================================
# Comma-separated list of allowed origins
# Example: "https://skilllens.streamlit.app,https://app.yourdomain.com"
# For local dev, use: "http://localhost:8503,http://127.0.0.1:8503"
CORS_ORIGINS_RAW="http://localhost:8503,http://127.0.0.1:8503"

# ===========================================
# API Keys for AI-powered insights (Milestone 3)
# ===========================================
# At least one key required for AI recommendations
# Get keys from:
# - Gemini: https://aistudio.google.com/app/apikey
# - OpenAI: https://platform.openai.com/api-keys
# - Anthropic: https://console.anthropic.com/settings/keys

OPENAI_API_KEY=your_openai_key_here
ANTHROPIC_API_KEY=your_anthropic_key_here
GEMINI_API_KEY=your_gemini_key_here

# LLM Configuration
# Provider order for failover (comma-separated: OPENAI,ANTHROPIC,GEMINI)
# Use GEMINI first as it's most reliable in some environments
LLM_PROVIDERS=GEMINI,OPENAI,ANTHROPIC
# Timeout per provider call (seconds)
LLM_TIMEOUT_SECONDS=20
# Max tokens in LLM response
LLM_MAX_TOKENS=800

# ===========================================
# Feature Flags (Experimental - off by default)
# ===========================================
# Enable semantic skill matching with LangChain embeddings
ENABLE_SEMANTIC=false
# Enable JWT authentication for protected endpoints
ENABLE_JWT=false

# Security Settings (optional overrides)
# MAX_FILE_SIZE_MB=5
# MAX_TEXT_CHARS=100000

